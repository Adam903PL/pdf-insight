# Google AI Studio API key. Server-side only — never expose it to the frontend.
GEMINI_API_KEY=
# Exact frontend origin allowed by CORS: scheme://host[:port], no path, no trailing slash.
# Local: http://localhost:5173 · Production: https://<github-user>.github.io
ALLOWED_ORIGIN=
PORT=3000
